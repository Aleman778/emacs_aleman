;;; packages.el --- define packages to use -*- lexical-binding: t; -*- 


;;(straight-use-package 'evil)         ;; extensible vi layer for emacs
(straight-use-package 'xah-fly-keys) ;; the most efficient keybinding for emacs 
(straight-use-package 'rg)           ;; ripgrep recursively searches directories for a regex pattern 
(straight-use-package 'company)      ;; modular in-buffer completion framework
(straight-use-package 'minions)      ;; hide all the minor-modes
